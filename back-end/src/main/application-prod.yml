# src/main/resources/application-prod.yml
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/tomportfolio_db?useSSL=false&serverTimezone=UTC
    username: root
    password: yA046861....
    driver-class-name: org.mariadb.jdbc.Driver

# JWT
app:
  jwt:
    secret: 32字节以上的随机密钥……
    ttlMillis: 3600000

# 如需在配置里维护 CORS，可自定义属性再在 CorsConfig 里读取
